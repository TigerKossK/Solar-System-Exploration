# Environment / secrets
.env
.env.*

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Dependencies
node_modules/

# Python bytecode (scripts/serve.py)
__pycache__/
*.pyc

# Design concept renders — ~15 MB of PNGs. Kept on disk for reference, but not
# tracked: they would otherwise be published with the site on GitHub Pages and
# make every clone 6x larger than the site itself.
Improvements/concepts/

# Local image-review contact sheet, regenerated by scripts/fetch-images.py.
# A working file for choosing imagery, not part of the site.
assets/media/review.html

# ---------------------------------------------------------------------------
# Local authoring tooling. None of this is part of the website: it is the
# scaffolding used to BUILD it. Kept on disk, deliberately not published — a
# visitor cloning the repo wants the site, not our working notes.
# ---------------------------------------------------------------------------

# AI agent / skill definitions vendored into the project (~200 files).
.claude/
.agents/
skills-lock.json

# Local review-tool output.
.impeccable/

# Working notes: improvement ideas, design specs, references, plans, and the
# original brief. Drafting material, superseded as the site itself changes.
Improvements/
plans/
Main Prompt1.md
PRODUCT.md